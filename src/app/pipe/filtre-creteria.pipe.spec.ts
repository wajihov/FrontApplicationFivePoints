import { FiltreCreteriaPipe } from './filtre-creteria.pipe';

describe('FiltreCreteriaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltreCreteriaPipe();
    expect(pipe).toBeTruthy();
  });
});
